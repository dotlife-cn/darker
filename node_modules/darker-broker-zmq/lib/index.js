/*jshint node:true */
'use strict';

var util = require('util');
var events = require('events');
var RepositoryBase = require('../../darker-core').cqrs.RepositoryBase;

var ZmqBroker = function(domainEventStorage, snapshotProvider, eventBroker) {
    RepositoryBase.call(this);

    this.domainEventStorage = domainEventStorage;
    this.snapshotProvider = snapshotProvider;
    this.eventBroker = eventBroker;
};
util.inherits(EventSourceRepository, RepositoryBase);

EventSourceRepository.prototype.get = function(aggregateRootId, aggregateRootType) {

};

EventSourceRepository.prototype.save = function(aggregateRoot) {

};

EventSourceRepository.prototype.commit = function() {

};

exports = module.exports = EventSourceRepository;